import React, { useEffect, useState } from 'react'
import { createRoot } from 'react-dom/client'
import { BrowserRouter, Link, Route, Routes, useNavigate, useParams } from 'react-router-dom'
import './styles.css'
import { whatsappNumber } from './config'

const fallbackProducts = [
  { id: 'rose-midi', slug: 'rose-garden-midi', name: 'Rose Garden Midi', collection: 'Summer Edit', price: 4890, description: 'A softly structured midi dress in a rose garden print, finished with a square neckline and a flowing skirt.', sizes: ['XS', 'S', 'M', 'L'], colours: ['Rose', 'Ivory'], stock: { XS: 3, S: 7, M: 5, L: 2 }, image: 'https://images.unsplash.com/photo-1566174053879-31528523f8ae?auto=format&fit=crop&w=1000&q=85', featured: true, published: true },
  { id: 'saffron-silk', slug: 'saffron-silk-slip', name: 'Saffron Silk Slip', collection: 'Occasion', price: 7290, description: 'An elegant bias-cut satin slip dress with a low back and delicate adjustable straps.', sizes: ['S', 'M', 'L'], colours: ['Saffron'], stock: { S: 4, M: 6, L: 3 }, image: 'https://images.unsplash.com/photo-1595777457583-95e059d581b8?auto=format&fit=crop&w=1000&q=85', featured: true, published: true },
  { id: 'linen-wrap', slug: 'linen-wrap-dress', name: 'Linen Wrap Dress', collection: 'Everyday Ease', price: 3990, description: 'Breathable linen with a flattering adjustable wrap waist for effortless days out.', sizes: ['XS', 'S', 'M', 'L', 'XL'], colours: ['Oat', 'Terracotta'], stock: { XS: 4, S: 8, M: 8, L: 4, XL: 2 }, image: 'https://images.unsplash.com/photo-1509631179647-0177331693ae?auto=format&fit=crop&w=1000&q=85', featured: false, published: true },
]

const money = (value) => new Intl.NumberFormat('en-IN', { style: 'currency', currency: 'INR', maximumFractionDigits: 0 }).format(value)
const api = async (path, options) => { const response = await fetch(`/api/${path}`, options); if (!response.ok) throw new Error((await response.json().catch(() => ({}))).error || 'Something went wrong'); return response.json() }
const whatsappOrderUrl = (product) => {
  const message = `Hello PAPA'S BOUTIQUE, I would like to order this dress:\n\n*${product.name}*\nCollection: ${product.collection}\nPrice: ${money(product.price)}\nProduct: ${window.location.href}`
  return `https://wa.me/${whatsappNumber}?text=${encodeURIComponent(message)}`
}

function Header() { return <header><Link className="brand logo" to="/" aria-label="PAPA'S BOUTIQUE home"><img src="/papa-boutique-logo.png" alt="PAPA'S BOUTIQUE" /></Link><nav><a href="#collections">Collections</a></nav></header> }
function ProductCard({ product }) { return <Link className="card" to={`/product/${product.slug}`}><img src={product.image} alt={product.name} /><div><p>{product.collection}</p><h3>{product.name}</h3><strong>{money(product.price)}</strong></div></Link> }
function Search({ value, onChange }) { return <label className="search"><span aria-hidden="true">⌕</span><input value={value} onChange={(event) => onChange(event.target.value)} placeholder="Search dresses or collections" aria-label="Search dresses or collections" /></label> }
function Home() { const [products, setProducts] = useState(fallbackProducts); const [filter, setFilter] = useState('All'); const [search, setSearch] = useState(''); useEffect(() => { api('products').then(({ products }) => setProducts(products)).catch(() => {}) }, []); const collections = ['All', ...new Set(products.map((product) => product.collection))]; const visible = products.filter((product) => { const matchesCollection = filter === 'All' || product.collection === filter; const query = search.trim().toLowerCase(); const matchesSearch = !query || [product.name, product.collection, ...(product.colours || [])].join(' ').toLowerCase().includes(query); return matchesCollection && matchesSearch }); return <><section className="hero"><div><p className="eyebrow">The new summer edit</p><h1>Dresses made for<br />beautiful moments.</h1><p className="lede">Thoughtfully chosen silhouettes, small-batch details, and pieces you will reach for again.</p><a className="button" href="#collections">Explore the collection</a></div></section><main id="collections"><div className="section-heading"><div><p className="eyebrow">Shop the boutique</p><h2>New arrivals</h2></div><Search value={search} onChange={setSearch} /></div><div className="filters">{collections.map((collection) => <button onClick={() => setFilter(collection)} className={filter === collection ? 'active' : ''} key={collection}>{collection}</button>)}</div>{visible.length ? <div className="grid">{visible.map((product) => <ProductCard product={product} key={product.id} />)}</div> : <p className="empty">No dresses match that search.</p>}</main></> }
function ProductDetail() { const { slug } = useParams(); const [product, setProduct] = useState(null); useEffect(() => { api(`products/${slug}`).then(({ product }) => setProduct(product)).catch(() => setProduct(fallbackProducts.find((item) => item.slug === slug))) }, [slug]); if (!product) return <main className="notice">Loading dress details…</main>; const totalStock = Object.values(product.stock || {}).reduce((total, qty) => total + Number(qty), 0); return <main className="product"><img src={product.image} alt={product.name} /><section><Link className="back" to="/">← Back to collection</Link><p className="eyebrow">{product.collection}</p><h1>{product.name}</h1><h2>{money(product.price)}</h2><p className="description">{product.description}</p><div className="detail"><span>Available sizes</span><div>{product.sizes.map((size) => <b key={size}>{size}</b>)}</div></div><div className="detail"><span>Colours</span><p>{product.colours.join(' · ')}</p></div><p className={totalStock ? 'in-stock' : 'sold-out'}>{totalStock ? `${totalStock} pieces currently in stock` : 'Currently sold out'}</p><a className="button whatsapp" href={whatsappOrderUrl(product)} target="_blank" rel="noreferrer"><svg viewBox="0 0 32 32" aria-hidden="true"><path d="M16 3a13 13 0 0 0-11.1 19.7L3 29l6.5-1.7A13 13 0 1 0 16 3Zm0 23.6a10.5 10.5 0 0 1-5.3-1.4l-.4-.2-3.8 1 1-3.7-.3-.4A10.5 10.5 0 1 1 16 26.6Zm5.8-7.8c-.3-.2-1.9-.9-2.2-1-.3-.1-.5-.2-.7.2s-.8 1-1 1.2c-.2.2-.4.2-.7 0a8.6 8.6 0 0 1-2.5-1.5 9.3 9.3 0 0 1-1.7-2.1c-.2-.3 0-.5.1-.7l.5-.6c.2-.2.2-.4.3-.6 0-.2-.7-1.7-1-2.3-.3-.7-.6-.6-.8-.6h-.7c-.2 0-.6.1-.9.5s-1.2 1.1-1.2 2.8 1.2 3.3 1.4 3.5a12.3 12.3 0 0 0 4.7 4.2c.7.3 1.2.6 1.6.7.7.2 1.4.2 1.9.1.6-.1 1.9-.8 2.1-1.6.3-.8.3-1.5.2-1.6-.1-.2-.3-.3-.6-.5Z" fill="currentColor"/></svg>Order on WhatsApp</a></section></main> }
const blank = { name: '', collection: '', price: '', description: '', sizes: 'S, M, L', colours: '', stock: 'S:0, M:0, L:0', image: '', featured: false, published: true }
function Admin() { const [loggedIn, setLoggedIn] = useState(false); const [password, setPassword] = useState(''); const [products, setProducts] = useState([]); const [draft, setDraft] = useState(blank); const [editing, setEditing] = useState(null); const [message, setMessage] = useState(''); const navigate = useNavigate(); const load = () => api('admin/products').then(({ products }) => { setProducts(products); setLoggedIn(true) }).catch(() => setLoggedIn(false)); useEffect(load, []); const login = async (event) => { event.preventDefault(); try { await api('auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ password }) }); await load() } catch (error) { setMessage(error.message) } }; const save = async (event) => { event.preventDefault(); const toList = (value) => value.split(',').map((item) => item.trim()).filter(Boolean); const stock = Object.fromEntries(draft.stock.split(',').map((part) => part.trim().split(':')).filter(([size, quantity]) => size && quantity !== undefined).map(([size, quantity]) => [size.trim(), Number(quantity)])); try { await api('admin/products', { method: editing ? 'PUT' : 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ ...draft, id: editing?.id, slug: editing?.slug, price: Number(draft.price), sizes: toList(draft.sizes), colours: toList(draft.colours), stock }) }); setDraft(blank); setEditing(null); setMessage('Collection saved.'); load() } catch (error) { setMessage(error.message) } }; const edit = (product) => { setEditing(product); setDraft({ ...product, sizes: product.sizes.join(', '), colours: product.colours.join(', '), stock: Object.entries(product.stock).map(([size, quantity]) => `${size}:${quantity}`).join(', ') }); window.scrollTo({ top: 0, behavior: 'smooth' }) }; const remove = async (id) => { if (!confirm('Remove this item from the collection?')) return; await api(`admin/products/${id}`, { method: 'DELETE' }); load() }; if (!loggedIn) return <main className="login"><form onSubmit={login}><p className="eyebrow">Private area</p><h1>Boutique admin</h1><p>Sign in to manage collections and inventory.</p><label>Password<input type="password" required value={password} onChange={(event) => setPassword(event.target.value)} /></label>{message && <p className="error">{message}</p>}<button className="button">Sign in</button></form></main>; return <main className="admin"><div className="section-heading"><div><p className="eyebrow">Inventory workspace</p><h1>{editing ? 'Edit dress' : 'Add a new dress'}</h1></div><button className="text-button" onClick={async () => { await api('auth/logout', { method: 'POST' }); navigate('/') }}>Sign out</button></div><form className="editor" onSubmit={save}>{[['name', 'Dress name'], ['collection', 'Collection'], ['price', 'Price (₹)'], ['image', 'Image URL'], ['sizes', 'Sizes, separated by commas'], ['colours', 'Colours, separated by commas'], ['stock', 'Stock, e.g. S:4, M:2']].map(([key, label]) => <label key={key}>{label}<input required={key !== 'colours'} type={key === 'price' ? 'number' : 'text'} value={draft[key]} onChange={(event) => setDraft({ ...draft, [key]: event.target.value })} /></label>)}<label className="wide">Description<textarea required value={draft.description} onChange={(event) => setDraft({ ...draft, description: event.target.value })} /></label><label className="check"><input type="checkbox" checked={draft.published} onChange={(event) => setDraft({ ...draft, published: event.target.checked })} /> Publish on storefront</label><button className="button">{editing ? 'Update dress' : 'Add dress'}</button>{editing && <button type="button" className="text-button" onClick={() => { setEditing(null); setDraft(blank) }}>Cancel edit</button>}<p>{message}</p></form><h2>Current catalogue</h2><div className="inventory">{products.map((product) => <article key={product.id}><img src={product.image} alt="" /><div><p>{product.collection}</p><h3>{product.name}</h3><span>{Object.values(product.stock).reduce((sum, qty) => sum + Number(qty), 0)} in stock · {product.published ? 'Published' : 'Hidden'}</span></div><button onClick={() => edit(product)}>Edit</button><button className="danger" onClick={() => remove(product.id)}>Remove</button></article>)}</div></main> }
function App() { return <><Header /><Routes><Route path="/" element={<Home />} /><Route path="/product/:slug" element={<ProductDetail />} /><Route path="/admin" element={<Admin />} /></Routes><footer>© {new Date().getFullYear()} PAPA'S BOUTIQUE · Curated with care</footer></> }
createRoot(document.getElementById('root')).render(<BrowserRouter><App /></BrowserRouter>)
